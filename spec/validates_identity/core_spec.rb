# frozen_string_literal: true

RSpec.describe ValidatesIdentity::Core do
  it "has a version number" do
    expect(ValidatesIdentity::Core::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
